package com.footballmanagergamesimulator.animation;

/** Mandatory, immutable outcome of the canonical moment. The animation engine
 *  renders it; it can never change it. */
public enum AnimationOutcome {
    GOAL, SAVE, MISS, BLOCKED
}
