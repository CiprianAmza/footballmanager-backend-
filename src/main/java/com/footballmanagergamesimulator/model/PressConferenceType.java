package com.footballmanagergamesimulator.model;

/**
 * Kind of a Press Conference V2 session. PRE_MATCH is generated before a
 * fixture the human team plays; POST_MATCH after it, with result-aware context.
 */
public enum PressConferenceType {
    PRE_MATCH,
    POST_MATCH
}
